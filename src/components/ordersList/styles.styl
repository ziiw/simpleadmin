.list
  .ordersHeader, .order
    display flex
    .details
      flex 0 0 16.6%
      padding 8px 12px
      box-sizing border-box
      border-bottom 1px solid rgba(0,0,0,0.15)
      text-align center
      display flex
      align-items center